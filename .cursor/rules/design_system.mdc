---
description: RigOS Design System
alwaysApply: true
---

Use the RigOS design system everywhere.

Never:

- hardcode colors
- hardcode spacing
- hardcode shadows
- hardcode typography

Always use:

- design tokens
- shared UI primitives
- existing animations
- existing components

If a new design primitive is needed:

Prefer extending the design system instead of implementing page-specific styles.